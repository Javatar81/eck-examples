apiVersion: v2
name: eck-stack
description: Elasticsearch + Kibana via ECK Stack Helm chart (single-node or multi-node via values)
type: application
version: 0.2.0
appVersion: "9.4.2"
dependencies:
  - name: eck-stack
    version: "0.19.0"
    repository: https://helm.elastic.co
